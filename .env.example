# VPN Rotator Service Configuration
# All environment variables use the VPN_ROTATOR_ prefix

# Required: Hetzner Cloud Configuration
VPN_ROTATOR_HETZNER_API_TOKEN=your_hetzner_token_here
VPN_ROTATOR_HETZNER_SSH_KEY=/path/to/your/ssh/public/key.pub
VPN_ROTATOR_HETZNER_SSH_PRIVATE_KEY_PATH=/path/to/your/ssh/private/key

# Optional: Hetzner Provider Settings
VPN_ROTATOR_HETZNER_SERVER_TYPES=cx11,cx21
VPN_ROTATOR_HETZNER_LOCATIONS=nbg1,fsn1
VPN_ROTATOR_HETZNER_IMAGE=ubuntu-22.04

# Optional: Logging Configuration
VPN_ROTATOR_LOG_LEVEL=info
VPN_ROTATOR_LOG_FORMAT=json

# Optional: API Server Configuration
VPN_ROTATOR_API_LISTEN_ADDR=:8080

# Optional: Database Configuration
VPN_ROTATOR_DB_PATH=./data/rotator.db
VPN_ROTATOR_DB_MAX_OPEN_CONNS=25
VPN_ROTATOR_DB_MAX_IDLE_CONNS=5
VPN_ROTATOR_DB_CONN_MAX_LIFETIME=300

# Optional: Service Configuration
VPN_ROTATOR_SERVICE_SHUTDOWN_TIMEOUT=30s

# Optional: Scheduler Configuration
VPN_ROTATOR_SCHEDULER_ROTATION_INTERVAL=24h
VPN_ROTATOR_SCHEDULER_CLEANUP_INTERVAL=1h
VPN_ROTATOR_SCHEDULER_CLEANUP_AGE=2h

# Connector Configuration (for client usage)
# VPN_ROTATOR_API_URL=http://localhost:8080
# VPN_ROTATOR_KEY_PATH=~/.vpn-rotator/client.key
# VPN_ROTATOR_INTERFACE=wg0
# VPN_ROTATOR_ALLOWED_IPS=0.0.0.0/0
# VPN_ROTATOR_DNS=1.1.1.1,8.8.8.8
# VPN_ROTATOR_POLL_INTERVAL=15
# VPN_ROTATOR_RETRY_ATTEMPTS=3
# VPN_ROTATOR_RETRY_DELAY=5
